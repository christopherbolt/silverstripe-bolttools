// JavaScript Document
(function ( $ ) {
	App.ready(function() {
		App.resize(function() {
			//setupForWindowSize();
		});
		//setupForWindowSize();
	});
	
	/*function setupForWindowSize () {
		var windowWidth = $(window).width();
		var windowHeight = $(window).height();
	}*/
}( jQuery ));